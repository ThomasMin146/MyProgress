<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Frame background color -->
    <stroke
        android:width="2dp"
        android:color="@color/red"/>   <!-- Border color -->
    <corners
        android:radius="8dp" /> <!-- Corner radius (optional) -->
</shape>
